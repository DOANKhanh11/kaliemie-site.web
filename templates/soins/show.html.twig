{% extends 'base.html.twig' %}

{% block title %}Soins{% endblock %}

{% block body %}
    <h1>Soins</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCategSoins</th>
                <td>{{ soin.idCategSoins }}</td>
            </tr>
            <tr>
                <th>IdTypeSoins</th>
                <td>{{ soin.idTypeSoins }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ soin.id }}</td>
            </tr>
            <tr>
                <th>Libel</th>
                <td>{{ soin.libel }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ soin.description }}</td>
            </tr>
            <tr>
                <th>Coefficient</th>
                <td>{{ soin.coefficient }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ soin.date ? soin.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_soins_index') }}">back to list</a>

    <a href="{{ path('app_soins_edit', {'idCategSoins': soin.idCategSoins}) }}">edit</a>

    {{ include('soins/_delete_form.html.twig') }}
{% endblock %}
