<form method="post" action="{{ path('app_soins_delete', {'idCategSoins': soins.idCategSoins}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ soins.idCategSoins) }}">
    <button class="btn">Delete</button>
</form>
